/** Fallback YAML rules shown if policy API is unreachable */
export const DEMO_POLICY_RULES = [
  {
    id: 'block_critical_external',
    description: 'Block external destination IPs on critical ML alerts',
    enabled: true,
    match: { min_ensemble_score: 0.85, labels: ['PortScan', 'DDoS', 'C2', 'Exfiltration', 'BruteForce'] },
    action: 'block_ip',
    auto_execute: false,
    require_approval: true,
  },
  {
    id: 'auto_isolate_lateral',
    description: 'Auto-isolate process on lateral movement / infiltration',
    enabled: true,
    match: { min_ensemble_score: 0.8, labels: ['Infiltration'] },
    action: 'isolate_process',
    auto_execute: true,
    require_approval: false,
  },
  {
    id: 'auto_isolate_portscan',
    description: 'Auto-isolate aggressive port scanners',
    enabled: true,
    match: { min_ensemble_score: 0.88, labels: ['PortScan'] },
    action: 'isolate_process',
    auto_execute: true,
    require_approval: false,
  },
  {
    id: 'block_bruteforce_ssh',
    description: 'Block destination on SSH brute-force patterns',
    enabled: true,
    match: { min_ensemble_score: 0.75, labels: ['BruteForce'], dst_ports: [22] },
    action: 'block_ip',
    auto_execute: false,
    require_approval: true,
  },
  {
    id: 'throttle_ddos',
    description: 'Rate-limit flood sources',
    enabled: true,
    match: { min_ensemble_score: 0.9, labels: ['DDoS'] },
    action: 'throttle',
    auto_execute: true,
    require_approval: false,
  },
  {
    id: 'alert_only_medium',
    description: 'Log medium-risk alerts for analyst review',
    enabled: true,
    match: { min_ensemble_score: 0.5, max_ensemble_score: 0.7 },
    action: 'alert_only',
    auto_execute: true,
    require_approval: false,
  },
];
